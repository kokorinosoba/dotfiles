#!/usr/bin/env bash

set -eu

git config --local user.name kokorinosoba
git config --local user.email 34607448+kokorinosoba@users.noreply.github.com
git config commit.template $HOME/dotfiles/.commit_template
