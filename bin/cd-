#!/bin/bash

N=${1:--1}
DIR=`pwd`
DIR=`basename ${DIR}`

NAME=`echo $DIR | sed -E "s/[0-9]*$//"`
NUM=`echo $DIR | sed -E "s/^[A-Za-z]*//"`

NUM=$((10#$NUM + N))
NUM=`printf %03d $NUM`

cd ../$NAME$NUM
